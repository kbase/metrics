metrics:
  image: test_build    
  environment:
    MONGO_PWD: ${MONGO_PWD}
    METRICS_USER_TOKEN: ${METRICS_USER_TOKEN}
    METRICS_MYSQL_PWD: ${METRICS_MYSQL_PWD}
    SQL_HOST: ${SQL_HOST}
    QUERY_ON: ${QUERY_ON}
